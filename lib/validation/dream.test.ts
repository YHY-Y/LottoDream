import {describe,expect,it} from 'vitest';import {dreamInputSchema,dreamResultSchema} from './dream';import {mockResult} from '@/lib/ai/gemini';describe('dream schemas',()=>{it('rejects short and oversized inputs',()=>{expect(()=>dreamInputSchema.parse({dream:'짧음'})).toThrow();expect(()=>dreamInputSchema.parse({dream:'꿈'.repeat(501)})).toThrow()});it('accepts the structured mock response',()=>expect(dreamResultSchema.parse(mockResult)).toEqual(mockResult))});
