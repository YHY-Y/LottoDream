import type {Metadata} from 'next';import {notFound} from 'next/navigation';import Link from 'next/link';import {dreamContents,type DreamSlug} from '@/data/dreams';import {AdSlot} from '@/components/ad-slot';
export function generateStaticParams(){return Object.keys(dreamContents).map(slug=>({slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const d=dreamContents[slug as DreamSlug];if(!d)return{};return{title:d.title,description:d.intro,alternates:{canonical:`/dreams/${slug}`}}}
export default async function DreamPage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const d=dreamContents[slug as DreamSlug];if(!d)notFound();return <article className="mx-auto max-w-3xl px-4 py-16"><p className="text-sm font-bold text-violet">꿈 해몽 사전</p><h1 className="mt-3 text-4xl font-black leading-tight text-night">{d.title}</h1><p className="mt-6 text-lg leading-8 text-slate-600">{d.intro}</p><AdSlot label="콘텐츠 중간 광고 영역"/><h2 className="mt-12 text-2xl font-black">상황별 해몽</h2><div className="mt-5 space-y-4">{d.situations.map(([title,text])=><section key={title} className="rounded-2xl border border-violet-100 bg-white p-6"><h3 className="font-extrabold text-night">{title}</h3><p className="mt-2 leading-7 text-slate-600">{text}</p></section>)}</div><div className="mt-12 rounded-3xl bg-night p-8 text-center text-white"><h2 className="text-2xl font-black">내 꿈은 어떤 의미일까요?</h2><p className="mt-3 text-sm text-indigo-200">같은 상징도 상황과 감정에 따라 달라집니다.</p><Link href="/#dream" className="mt-6 inline-block rounded-full bg-gold px-6 py-3 font-bold text-night">내 꿈 직접 해석하기</Link></div><p className="mt-8 text-xs leading-5 text-slate-500">이 글은 문화적·상징적 관점의 오락용 정보이며 미래, 건강, 재산상의 결과를 예측하지 않습니다.</p></article>}
